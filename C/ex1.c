#include <stdio.h>
#define TEXT "JAVA Web Application Class Fighting~!!"
int main(void) {
	printf("저의 \\이름은 \"김기태\"이며, \n나이는 29세\t입니다.\a");
	printf(TEXT);
	return 0;	
//	언어번역기에 따른 프로그래밍언어의 종류 : 어셈블러, 컴파일러, 인터프리터 
//	이스케이프 문자(출력 제어용 문자) 
//  \'(작은 따옴표), \"(큰 따옴표), \?(물음표), \\(백슬래시), \a(경고음)
//	\t(탭), \n(줄바꿈)
//	\b(백스페이스), \r(캐리지리턴), \f(폼피드 - 한 화면씩 이동) 
} 
